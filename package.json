{
  "name": "amarzeus-profile",
  "version": "2.0.0",
  "description": "World-class GitHub profile with AI-powered features",
  "scripts": {
    "dev": "vercel dev",
    "build": "vercel build",
    "deploy": "vercel --prod",
    "format": "prettier --write \"**/*.{html,j,css,md}\""
  },
  "dependencies": {
    "three": "^0.158.0"
  },
  "devDependencies": {
    "@vercel/node": "^5.6.9",
    "prettier": "^3.0.0"
  }
}
